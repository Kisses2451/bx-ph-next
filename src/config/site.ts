export const siteName = 'Bloodlust Philippines'

/** Year the club was founded, shown in the home hero ("Est. …") and the About page stats. TODO: confirm (the old About copy said 2025). */
export const siteFoundedYear = '2021'

/** Links in the footer's "Follow" column, e.g. { label: 'Facebook', href: 'https://facebook.com/…' }. The column is hidden while this list is empty. */
export const socialLinks: { label: string; href: string }[] = []

/**
 * Where "Become a partner" / "Your logo here" on the Partners page lead, e.g. 'mailto:partners@example.com'
 * or a contact form URL. Both are hidden while this is empty.
 */
export const partnershipContactHref = ''
